name: macos 
on: [push]
jobs:
  check-bats-version:
    runs-on: macos-latest 
    steps:
      - uses: actions/checkout@v3
      - name: build
        run: |
            pip list
            python3 test.py
